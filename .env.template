# Copy this file to .env.local and adjust the values as needed.
# .env.local is gitignored and overrides these defaults.

# Base URL the brand assets (db.png, logo.png, ...) are served from.
VITE_ASSETS_BASE_URL=
